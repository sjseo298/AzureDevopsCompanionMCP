{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["docker", "run", "-i", "--rm", "--network", "host", "--user", "root", "mcp/playwright"]
    }
  }
}
